module.exports = {
  devServer: {
    host: 'localhost',
    port: 3000 // override the local port (localhost:3000) when running 'npm run start' in terminal; can always change port value. 
  }
};

// configuration value; something used on your local machine 